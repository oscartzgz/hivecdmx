= turbo_stream.replace @record.frame_id do
  = render "records/record",
           record:   @record,
           item:     @checklist_item,
           room:     @record.room,
           category: @record.category
